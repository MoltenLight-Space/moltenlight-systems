# Models

Capella and SysML v2 models will be populated starting **Phase A**.

Capella will hold the primary project model under `capella/`, with
subsystem models added in Phase B via the System-to-Subsystem Transition
add-on. SysML v2 via Eclipse SysON will hold a parallel learning-track
model for one subsystem under `sysml-v2/`.

Nothing here yet.
